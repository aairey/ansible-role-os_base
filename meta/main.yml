---
galaxy_info:
  role_name: os_base
  author: aairey
  description: Role to prepare OS for further consumption by myself

  license: GPL-3.0-only

  min_ansible_version: 2.10

  platforms:
    - name: Fedora
      versions:
        - 30
        - 33
        - 35

  galaxy_tags:
    - system

dependencies:
  #  - name: markosamuli.linuxbrew
  #    when: fedora_edition == 'workstation' or fedora_edition == 'toolbox'
  #  - name: cimion-io.asdf
  #    when: fedora_edition == 'workstation' or fedora_edition == 'toolbox'
  #  - name: jaredhocutt.gnome_extensions
  #    when: not fedora_edition == 'server'
